# =============================================================================
# Proxy-Hunter — requirements.txt
# Все зависимости проекта в одном файле.
# Python >= 3.11
# =============================================================================

# -----------------------------------------------------------------------------
# Асинхронный HTTP / сеть
# -----------------------------------------------------------------------------
# Основной HTTP-клиент: fetch_configs.py, session_pool.py, active_checker.py
aiohttp>=3.9.5,<4.0

# Асинхронный DNS-резолвер: xray_balancer.py (замена socket.getaddrinfo)
aiodns>=3.2.0

# Ускоренный event loop для Linux (CI = ubuntu).
# На Windows не устанавливается — обёрнут в try/except в точке входа.
uvloop>=0.19.0; sys_platform != 'win32'

# -----------------------------------------------------------------------------
# Асинхронные файловые операции
# -----------------------------------------------------------------------------
# Чтение/запись конфигов, архивов, кэшей без блокировки event loop
aiofiles>=23.2.1

# -----------------------------------------------------------------------------
# Хеширование и дедупликация
# -----------------------------------------------------------------------------
# Быстрое хеширование для deep_deduplicate.py и bloom_async.py
xxhash>=3.4.1

# -----------------------------------------------------------------------------
# Парсинг и валидация
# -----------------------------------------------------------------------------
# YAML-парсинг: Clash-конфиги, Xray outbound configs (xray_balancer.py)
PyYAML>=6.0.1

# HTML-парсинг: fetch_configs.py (извлечение конфигов из Telegram-страниц)
beautifulsoup4>=4.12.3

# Быстрый XML/HTML парсер-бэкенд для BeautifulSoup
lxml>=5.1.0

# Синхронный HTTP-клиент (fallback, install.sh проверки)
requests>=2.31.0

# -----------------------------------------------------------------------------
# Прогресс и логирование
# -----------------------------------------------------------------------------
# Прогресс-бары для пайплайна (pipeline_optimized.py)
tqdm>=4.66.2

# -----------------------------------------------------------------------------
# ML / Скоринг / Feature Engineering
# -----------------------------------------------------------------------------
# Числовые вычисления: feature_extractor.py, survival_model.py
numpy>=1.26.0,<3.0

# Работа с табличными данными: feature_extractor.py, train_model.py
pandas>=2.2.0

# ML-утилиты: train_model.py (метрики, split, preprocessing)
scikit-learn>=1.4.0

# Градиентный бустинг: train_model.py (основная модель скоринга)
catboost>=1.2.3

# -----------------------------------------------------------------------------
# Утилиты
# -----------------------------------------------------------------------------
# Работа с переменными окружения и .env файлами (user_settings.py)
python-dotenv>=1.0.1

# Типизация для Python < 3.12 (если запуск на 3.11)
typing-extensions>=4.10.0; python_version < '3.12'
