# =============================================================================
# Proxy-Hunter — requirements.txt
# ИСЧЕРПЫВАЮЩИЙ список всех зависимостей проекта.
# Python >= 3.11
# =============================================================================

# -----------------------------------------------------------------------------
# 1. АСИНХРОННЫЙ HTTP / СЕТЬ
#    fetch_configs.py, session_pool.py, active_checker.py,
#    multi_level_verifier.py, region_tester.py, pipeline_optimized.py
# -----------------------------------------------------------------------------
aiohttp>=3.9.5,<4.0

# Асинхронный DNS-резолвер (замена блокирующего socket.getaddrinfo)
# xray_balancer.py, region_tester.py
aiodns>=3.2.0

# Ускоренный event loop для Linux/macOS.
# На Windows НЕ устанавливается. В коде обёрнут в try/except ImportError.
uvloop>=0.19.0; sys_platform != 'win32'

# -----------------------------------------------------------------------------
# 2. АСИНХРОННЫЕ ФАЙЛОВЫЕ ОПЕРАЦИИ
#    pipeline_optimized.py, db.py
# -----------------------------------------------------------------------------
aiofiles>=23.2.1

# -----------------------------------------------------------------------------
# 3. ХЕШИРОВАНИЕ И ДЕДУПЛИКАЦИЯ
#    deep_deduplicate.py, bloom_async.py
# -----------------------------------------------------------------------------
xxhash>=3.4.1

# Bloom-фильтр для дедупликации (bloom_async.py: from pybloom_live import ScalableBloomFilter)
# Транзитивно тянет bitarray, mmh3
pybloom-live>=4.0.0

# -----------------------------------------------------------------------------
# 4. ПАРСИНГ КОНТЕНТА
# -----------------------------------------------------------------------------
# HTML-парсинг Telegram-страниц (fetch_configs.py: from bs4 import BeautifulSoup)
beautifulsoup4>=4.12.3

# Быстрый XML/HTML парсер-бэкенд для BeautifulSoup
lxml>=5.1.0

# YAML-парсинг: Clash-конфиги, Xray outbounds (xray_balancer.py)
PyYAML>=6.0.1

# Парсинг дат из Telegram-сообщений (fetch_configs.py: from dateutil import parser)
python-dateutil>=2.9.0

# Синхронный HTTP-клиент (fallback, install.sh)
requests>=2.31.0

# -----------------------------------------------------------------------------
# 5. ПРОГРЕСС И ВИЗУАЛИЗАЦИЯ
#    pipeline_optimized.py
# -----------------------------------------------------------------------------
tqdm>=4.66.2

# -----------------------------------------------------------------------------
# 6. ML / СКОРИНГ / FEATURE ENGINEERING
#    feature_extractor.py, train_model.py
# -----------------------------------------------------------------------------
numpy>=1.26.0,<3.0
pandas>=2.2.0
scikit-learn>=1.4.0
catboost>=1.2.3

# -----------------------------------------------------------------------------
# 7. КОНФИГУРАЦИЯ И СРЕДА
# -----------------------------------------------------------------------------
# Загрузка .env файлов (user_settings.py)
python-dotenv>=1.0.1

# Бэкпорт типов для Python 3.11
typing-extensions>=4.10.0; python_version < '3.12'

# -----------------------------------------------------------------------------
# 8. СЕРИАЛИЗАЦИЯ
# -----------------------------------------------------------------------------
# Быстрая бинарная сериализация (bloom_async.py, region_tester.py)
msgpack>=1.0.7

# -----------------------------------------------------------------------------
# 9. ЛОГИРОВАНИЕ
# -----------------------------------------------------------------------------
python-json-logger>=2.0.7

# =============================================================================
# ПРОВЕРКА УСТАНОВКИ (в CI):
#   python -c "
#   import aiohttp, aiofiles, xxhash, tqdm, yaml, requests
#   import bs4, lxml, numpy, pandas, sklearn, catboost
#   import aiodns, dotenv, msgpack, pybloom_live, dateutil
#   print('All dependencies OK')
#   "
# =============================================================================
